# PickleTV portable config (edit as needed)
# This file is sourced by tools/push_video.sh
# You can override any variable via environment or by editing this file.

# Android application package
PACKAGE_NAME="com.example.pickletv"

# Video file to use during debug. Resolved relative to repo root if not absolute.
VIDEO_FILE_NAME="h-6.mp4"

# Fallback generation control
FALLBACK_ENABLE_GENERATION="1"      # 1 to auto-generate when VIDEO_FILE missing
FALLBACK_FILE_NAME="h-6_720p30.mp4"
FALLBACK_DURATION_SEC="10"
FALLBACK_SIZE="1280x720"
FALLBACK_FPS="30"

# Destination directory on device
DEST_DIR="/sdcard/Android/data/${PACKAGE_NAME}/cache"
# Destination full path on device
DEST_PATH="${DEST_DIR}/${VIDEO_FILE_NAME}"

